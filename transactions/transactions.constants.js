const EXPENSE = `EXPENSE`;
const INCOME = `INCOME`;
const TRANSFER = `TRANSFER`;
const TransactionsTypeEnum = [EXPENSE, INCOME, TRANSFER];

module.exports = {
  EXPENSE,
  INCOME,
  TRANSFER,
  TransactionsTypeEnum,
};
